[project]
name = "rendercv-resume-template"
version = "0.1.0"
description = "A reusable, version-controlled RenderCV resume template"
requires-python = ">=3.12,<3.15"
dependencies = [
  "pypdf==6.16.1",
  "rendercv[full]==2.8",
]

[tool.uv]
package = false
required-version = "==0.12.5"
